@model IEnumerable<LaptopType>
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Category List</h2>
        </div>
        <div class="col-6 text-end">
            <a
                asp-controller="LaptopType"
                asp-action="Create"
                class="btn btn-primary"
            >
                <i class="bi bi-patch-plus"></i>
                &nbsp; New Laptop Type
            </a>
        </div>
    </div>


    <div class="mt-4"> 
        <table class="table table-bordered table-striped" style="width: 100%;">
        <thead>
            <tr>
                <th>Laptop Type</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var laptopType in Model)
            {
                <tr>
                    <td>@laptopType.Name</td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="LaptopType" asp-action="Edit" asp-route-id="@laptopType.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-fill"></i>Edit 
                            </a>
                            
                            <a asp-controller="LaptopType" asp-action="Destroy" asp-route-id="@laptopType.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash3-fill"></i>Delete 
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    </div>

</div>